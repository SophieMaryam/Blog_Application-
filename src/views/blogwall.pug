doctype
html 
	head
		title Wall
		link(rel="stylesheet" href="./main.css" type="text/css")
		link(href="https://fonts.googleapis.com/css?family=Raleway" rel="stylesheet")
		link(href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet")
		link(rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.99.0/css/materialize.min.css")
		meta(name="viewport" content="width=device-width, initial-scale=1.0")
	body
		div#allblogs
			h1#walltitle The Wall 
			each post in info
				each user in users
					if post.userId === user.id
						p #{user.username} wrote "#{post.title}"
						p #{post.body}
						div.comments
							each comment in post.comments
								each user in users
									if comment.userId === user.id
										p.comment
											i #{user.username} commented:
											#{comments.comments}  
											//- comments.comments (alias.columnname)
							form(name="form" method="post" action="/comments")
								label Comment:
								input#password(type="text" name="comments" placeholder="Comment..." required)
								input(type='hidden' name='blogId' value=post.id)
								br
								br
								button(type="submit") Submit 

			a(href='/myposts')
				button.button(type=button) Return Back
				


		script(type="text/javascript" src="https://code.jquery.com/jquery-3.2.1.min.js")
		script(type="text/javascript" src="js/materialize.min.js")


		